
# FEATURE: allow not only recording of a new video, but also using the exising video from Photos gallery. Also limit the video to 10s.

# FEATURE: Allow to stop camera recording before the full 10 seconds have passed. Sometimes users will want to stop the recording after they were lucky to capture a great moment of their cat, and don't want to spoil it with what will happen next. Currently when recording I see a circle on the screen displaying number of seconds left - we should allow tapping on that circle to end the recording prematurely. But we need to make it obvious for user that they can do this - tap and finish the recording.

# FEATURE: Camera recording - allow multiple takes, add buttons on the bottom "Use it" and "Retake"
